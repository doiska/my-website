---
import type { CollectionEntry } from 'astro:content';
import BaseBody from '../components/base-body.astro';
import BaseHead from '../components/base-head.astro';
import Header from '../components/Header.astro';
import Newsletter from '../components/newsletter.astro';
import FormattedDate from '../components/FormattedDate.astro';

type Props = CollectionEntry<'blog'>['data'];

const { title, description, pubDate, updatedDate, heroImage } = Astro.props;
---

<html lang="en">
	<head>
		<BaseHead title={title} description={description} />
	</head>

	<BaseBody>
		<Header />
		<main class="flex justify-center w-full max-w-3xl mx-auto">
			<article class="w-full max-w-full prose prose-neutral prose-h1:text-balance pb-10 dark:prose-invert">
				<div class="w-full">
					{heroImage && <img width={1020} height={510} src={heroImage} alt="" class="block mx-0 my-auto shadow-2xl rounded-lg" />}
				</div>
				<div class="mt-8">
					<div class="text-center font-jetbrains tracking-tighter">

						<h1>{title}</h1>
						<hr class="m-0" />
					</div>
					<slot />
          <hr class="m-0" />
					<!--<div class="flex justify-end">-->
					<!--	{pubDate && (-->
          <!--      <span>Published on <FormattedDate date={pubDate} /></span>-->
					<!--	)}-->
					<!--	{-->
					<!--			updatedDate && (-->
					<!--					<div class="last-updated-on">-->
					<!--						Last updated on <FormattedDate date={updatedDate} />-->
					<!--					</div>-->
					<!--			)-->
					<!--	}-->
					<!--</div>-->
				</div>
			</article>
		</main>
		<Newsletter />
	</BaseBody>
</html>
